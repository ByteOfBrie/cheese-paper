id: gay.brie.CheesePaper
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: "org.freedesktop.Sdk"
sdk-extensions:
  - "org.freedesktop.Sdk.Extension.rust-stable"
  - "org.freedesktop.Sdk.Extension.llvm22"
build-options:
  append-path: /usr/lib/sdk/rust-stable/bin
command: "cheese-paper"
finish-args:
  # standard desktop protocols
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  # GPU acceleration (for egui (I think?))
  - --device=dri
  # only for checking for updates, disabled on flathub
  - --share=network
modules:
  - name: cheese-paper
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/rust-stable/bin
      env:
        CARGO_HOME: /run/build/cheese-paper/cargo
        CARGO_NET_OFFLINE: 'true'
    build-commands:
      - cargo --offline fetch --manifest-path Cargo.toml --verbose
      - cargo --offline build --release --no-default-features --features polling,update_checking --verbose
      - install -Dm755 ./target/release/cheese-paper -t /app/bin
      - install -Dm644 ./build/flatpak/${FLATPAK_ID}.metainfo.xml -t /app/share/metainfo/
      - install -Dm644 ./build/${FLATPAK_ID}.desktop -t /app/share/applications/
      - install -Dm644 ./resources/cheese-paper-icon.png /app/share/icons/hicolor/256x256/apps/gay.brie.CheesePaper.png
    sources:
      - type: dir
        path: "../.."
      - "cargo-sources.json"
